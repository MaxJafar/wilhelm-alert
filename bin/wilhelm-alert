#!/usr/bin/env sh
# Thin shim to the cross-platform core. Kept as a shell entry point because
# existing hook configs (and the plugin manifests) point at this path.
# Windows uses wilhelm-alert.cmd instead.
exec node "$(dirname "$0")/wilhelm-alert.js" "$@"
